{% extends 'SitePlatformBundle::layout.html.twig' %}

{% block content %}
    <h1>Requirement edit</h1>

    {{ form(edit_form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('requirement') }}">
            Back to the list
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
